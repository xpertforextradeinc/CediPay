version: "3.9"

services:
  api:
    build: .
    container_name: cedipay-api
    ports:
      - "4000:4000"
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    container_name: cedipay-db
    environment:
      POSTGRES_USER: cedipay
      POSTGRES_PASSWORD: cedipay
      POSTGRES_DB: cedipay
    ports:
      - "5432:5432"
    volumes:
      - cedipay_data:/var/lib/postgresql/data

volumes:
  cedipay_data:
